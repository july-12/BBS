<main class="flex gap-10 flex-col sm:flex-row w-full" >
  <section style="height: fit-content" class="shadow-sm rounded-lg flex-1 flex flex-col gap-1 sm:gap-0">
    <% @posts.each do |post| %>
      <%= render  "posts/post", post: post %>
    <% end %>
  </section>
  <div class="sm:w-1/4 sm:max-w-[400px] flex flex-col gap-4">
    <div class="card bg-white shadow-sm">
      <div class="card-body p-4">
        <div class="card-action">
          <button class="w-full text-grey-400 btn btn-primary btn-sm">
            <%= link_to  "发布话题", user_signed_in? ? new_post_path : user_session_path %>
          </button>
        </div>
      </div>
    </div>
    <div class="card bg-white shadow-sm">
      <div class="card-body p-4">
        <h2 class="card-title text-lg">小贴士</h2>
        <p>一切都需要从先上传一个头像开始！</p>
      </div>
    </div>
    <div class="card bg-white shadow-sm">
      <div class="card-body p-4">
        <h2 class="card-title text-lg">社区规则</h2>
        <p>和谐、有爱</p>
      </div>
    </div>
  </div>
</main>